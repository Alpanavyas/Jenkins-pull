pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello'
                try{
                        git 'https://github.com/Alpanavyas/first-jenkins.git'
                }
                catch (err){
      echo err
   }
            }
        }
    }
}
